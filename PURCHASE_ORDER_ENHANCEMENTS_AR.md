# تحسينات أوامر الشراء - طريقة الدفع ورقم المشروع

## الملخص
تم إضافة حقلين مهمين إلى نموذج أوامر الشراء:

### 1. طريقة الدفع (Payment Method)
- **المصدر**: يتم جلب البيانات من القائمة الرئيسية (Master Data)
- **الرابط**: `/api/payment-methods`
- **النوع**: حقل اختياري (يمكن ترك الحقل فارغاً)
- **الفائدة**: بدلاً من الإدخال اليدوي، يتم اختيار طريقة الدفع من قائمة موحدة

### 2. رقم المشروع (Project Number)
- **النوع**: حقل إلزامي لاعتماد أمر الشراء
- **القاعدة**: لا يمكن اعتماد أمر الشراء إلا بعد إضافة رقم المشروع
- **الهدف**: ربط جميع عمليات الشراء بالمشاريع لتتبع التكاليف

## التغييرات في الواجهة

### موقع الحقول في النموذج
```
الصف 1: [المورد*]           [نوع الطلب]
الصف 2: [تاريخ الطلب*]       [تاريخ التسليم المتوقع]
الصف 3: [المشروع*]           [طريقة الدفع]
الصف 4: [ملاحظات] (العرض الكامل)
```

### شكل الحقول
- **المشروع**: قائمة منسدلة تعرض: رقم المشروع - اسم المشروع
- **طريقة الدفع**: قائمة منسدلة تعرض اسم طريقة الدفع (عربي/إنجليزي)
- **علامة * الحمراء**: تظهر بجانب حقل المشروع (حقل إلزامي)

## القواعد الجديدة

### عند إنشاء أمر شراء جديد
- ✅ يمكن حفظ أمر الشراء بدون اختيار المشروع (حالة مسودة)
- ✅ يمكن حفظ أمر الشراء بدون اختيار طريقة الدفع
- ⚠️ عند محاولة الاعتماد، يجب أن يكون المشروع محدداً

### عند اعتماد أمر الشراء
- ❌ **لا يمكن الاعتماد** إذا لم يتم اختيار المشروع
- ✅ رسالة الخطأ: "يجب تحديد المشروع قبل اعتماد أمر الشراء"
- ✅ يمكن الاعتماد بدون طريقة دفع (حقل اختياري)

## التحديثات التقنية

### قاعدة البيانات
```sql
-- تم إضافة عمودين جديدين
ALTER TABLE purchase_orders
ADD COLUMN payment_method_id INTEGER REFERENCES payment_methods(id);

ALTER TABLE purchase_orders
ADD COLUMN project_id INTEGER REFERENCES projects(id);
```

**الحالة**: ✅ تم تنفيذ التحديث بنجاح

### Backend (الخادم)
**الملف**: `backend/src/routes/procurement/purchaseOrders.ts`

التحديثات:
- ✅ إضافة `payment_method_id` و `project_id` في CREATE (إنشاء)
- ✅ إضافة `payment_method_id` و `project_id` في UPDATE (تحديث)
- ✅ إضافة فحص `project_id` في APPROVE (اعتماد):
  ```typescript
  if (!po.project_id) {
    return res.status(400).json({
      error: {
        code: 'PROJECT_REQUIRED',
        message: 'يجب تحديد المشروع قبل اعتماد أمر الشراء'
      }
    });
  }
  ```

### Frontend (الواجهة)
**الملف**: `frontend-next/pages/purchasing/orders.tsx`

التحديثات:
- ✅ إضافة حقل المشروع (إلزامي، علامة *)
- ✅ إضافة حقل طريقة الدفع (اختياري)
- ✅ جلب قائمة المشاريع من `/api/projects?status=active`
- ✅ جلب قائمة طرق الدفع من `/api/payment-methods`
- ✅ التحقق من صحة البيانات: المشروع مطلوب
- ✅ دعم اللغة العربية والإنجليزية

## الاختبار

### خطوات الاختبار المطلوبة
1. ✅ إنشاء أمر شراء جديد بدون مشروع → يجب أن ينجح (مسودة)
2. ✅ إنشاء أمر شراء مع المشروع وطريقة الدفع → يجب أن ينجح
3. ✅ محاولة اعتماد أمر شراء بدون مشروع → يجب أن يفشل مع رسالة خطأ
4. ✅ اعتماد أمر شراء مع المشروع → يجب أن ينجح
5. ✅ التحقق من ظهور قائمة طرق الدفع من البيانات الرئيسية
6. ✅ التحقق من ظهور قائمة المشاريع النشطة فقط

### رسائل الخطأ المتوقعة

**عند محاولة الاعتماد بدون مشروع**:
```
رقم المشروع مطلوب للاعتماد
```

**عند ترك حقل المشروع فارغاً عند الحفظ مع الاعتماد**:
```
يجب تحديد المشروع قبل اعتماد أمر الشراء
```

## المشاكل الشائعة والحلول

### المشكلة 1: قائمة طرق الدفع فارغة
**السبب**: لا توجد طرق دفع مضافة في البيانات الرئيسية
**الحل**: إضافة طرق الدفع من صفحة: البيانات الرئيسية ← طرق الدفع

### المشكلة 2: قائمة المشاريع فارغة
**السبب**: لا توجد مشاريع نشطة
**الحل**: إنشاء مشاريع جديدة والتأكد من أن الحالة "نشط"

### المشكلة 3: لا يمكن الاعتماد حتى مع اختيار المشروع
**السبب**: لم يتم حفظ رقم المشروع في قاعدة البيانات
**الحل**: التحقق من وحدة تحكم المتصفح (Console) للأخطاء

## الفوائد

### 1. تتبع المشاريع
- ربط جميع تكاليف الشراء بالمشاريع
- تقارير دقيقة عن تكلفة كل مشروع
- مراقبة ميزانية المشروع مقابل أوامر الشراء

### 2. معايير موحدة
- طرق دفع موحدة (بدلاً من الإدخال اليدوي)
- تقليل الأخطاء الإملائية
- تسهيل التقارير والإحصائيات

### 3. رقابة مالية
- لا يمكن اعتماد أوامر الشراء بدون ربطها بمشروع
- ضمان تتبع جميع التكاليف
- تحسين التحكم في الميزانية

## التحسينات المستقبلية

### قريباً
- [ ] اختبارات تلقائية للتحقق من القواعد
- [ ] لوحة تحكم خاصة بالمشاريع وأوامر الشراء
- [ ] تقارير تفصيلية عن التكاليف لكل مشروع

### لاحقاً
- [ ] مقارنة ميزانية المشروع مع أوامر الشراء
- [ ] إحصائيات عن طرق الدفع المستخدمة
- [ ] توقعات الشراء بناءً على المشاريع

## ملاحظات مهمة

### للمستخدمين
- 📌 يجب اختيار المشروع قبل اعتماد أي أمر شراء
- 📌 طريقة الدفع اختيارية ولكن يُنصح باختيارها
- 📌 قائمة المشاريع تعرض المشاريع النشطة فقط
- 📌 يمكن حفظ أمر الشراء كمسودة بدون اختيار المشروع

### للمطورين
- ✅ تم تنفيذ جميع التغييرات
- ✅ قاعدة البيانات محدثة
- ✅ Backend محدث
- ✅ Frontend محدث
- ⏳ يحتاج لإعادة تشغيل الخدمات
- ⏳ يحتاج للاختبار الكامل

## الحالة الحالية

| المكون | الحالة | الملاحظات |
|--------|--------|-----------|
| قاعدة البيانات | ✅ جاهز | Migration 131 تم تنفيذه |
| Backend API | ✅ جاهز | جميع endpoints محدثة |
| Frontend UI | ✅ جاهز | النموذج محدث بالحقول الجديدة |
| الاختبار | ⏳ قيد التنفيذ | يحتاج لإعادة تشغيل الخدمات |
| الوثائق | ✅ جاهز | ملفات التوثيق جاهزة |

## خطوات التشغيل

1. ✅ إنشاء ملف Migration
2. ✅ تنفيذ Migration على قاعدة البيانات
3. ✅ تحديث كود Backend
4. ✅ تحديث كود Frontend
5. ⏳ إعادة تشغيل Backend
6. ⏳ إعادة تشغيل Frontend
7. ⏳ اختبار التدفق الكامل
8. ⏳ تحديث دليل المستخدم

---

**آخر تحديث**: 2024-01-15
**الإصدار**: 1.0.0
**الحالة**: ✅ التطوير مكتمل - في انتظار الاختبار
