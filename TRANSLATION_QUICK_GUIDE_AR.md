# دليل الترجمة السريع

## ✅ تم الإنجاز

### الواجهات المترجمة بالكامل:
1. **الأصناف (Items)** - ✅ مكتمل
2. **الموردون (Vendors)** - ✅ مكتمل
3. **العملاء (Customers)** - ✅ مكتمل
4. **المستودعات (Warehouses)** - ✅ مكتمل
5. **فئات الأصناف (Item Categories)** - ✅ محدث

### الملفات المحدثة:
- ✅ `locales/en.json` - الترجمات الإنجليزية
- ✅ `locales/ar.json` - الترجمات العربية
- 📚 `locales/master-pages-translations.json` - مرجع شامل لجميع الواجهات

## 🚀 خطوات التطبيق

### 1. إعادة تشغيل الواجهة
قم بتشغيل الملف:
```
restart-frontend-translations.bat
```

أو باستخدام Docker مباشرة:
```bash
docker restart slms-frontend-next-1
```

### 2. اختبار الترجمات
1. افتح المتصفح: http://localhost:3001
2. قم بتجربة الواجهات التالية:
   - الأصناف: `/master/items`
   - الموردون: `/master/vendors`
   - العملاء: `/master/customers`
   - المستودعات: `/master/warehouses`
   - فئات الأصناف: `/master/item-categories`

## 📋 الواجهات المتبقية

جميع الواجهات الـ 70 المتبقية لها ترجمات جاهزة في الملف:
`locales/master-pages-translations.json`

### قائمة الواجهات:
- Address Types (أنواع العناوين)
- Allowances (البدلات)
- Approval Workflows (سير الموافقات)
- Backup Settings (إعدادات النسخ الاحتياطي)
- Bank Accounts (الحسابات البنكية)
- Batch Numbers (أرقام الدفعات)
- Border Points (نقاط الحدود)
- Chart of Accounts (دليل الحسابات)
- Cheque Books (دفاتر الشيكات)
- Clearing Agents (وكلاء التخليص)
- ... والمزيد (راجع الملف الكامل)

## 🔧 كيفية إضافة ترجمة لواجهة جديدة

### الخطوة 1: افتح الملف المرجعي
افتح `locales/master-pages-translations.json`

### الخطوة 2: انسخ القسم المطلوب
مثلاً، لإضافة ترجمة لـ "Bank Accounts":
```json
"bankAccounts": {
  "title": "Bank Accounts",
  "subtitle": "Manage company bank accounts",
  ...
}
```

### الخطوة 3: أضف للملفات الرئيسية
1. انسخ إلى `locales/en.json` (نهاية الملف، قبل `}` الأخير)
2. ترجم المحتوى وأضفه إلى `locales/ar.json`

### الخطوة 4: أعد تشغيل الحاوية
```
restart-frontend-translations.bat
```

## 📝 أمثلة الاستخدام في الكود

### مثال 1: عناوين الصفحة
```tsx
import { useTranslation } from '../../hooks/useTranslation';

function MyPage() {
  const { t } = useTranslation();
  
  return (
    <div>
      <h1>{t('items.title')}</h1>
      <p>{t('items.subtitle')}</p>
    </div>
  );
}
```

### مثال 2: أزرار
```tsx
<Button onClick={handleCreate}>
  <PlusIcon className="w-5 h-5 mr-2" />
  {t('items.new')}
</Button>
```

### مثال 3: حقول الإدخال
```tsx
<Input
  label={t('items.itemCode')}
  value={formData.code}
  onChange={(e) => setFormData({ ...formData, code: e.target.value })}
  error={formErrors.code}
  required
/>
```

### مثال 4: رسائل مع متغيرات
```tsx
const message = t('items.deleteMessage', { name: item.name });
// النتيجة: "Are you sure you want to delete "Item 1"? This action cannot be undone."
```

### مثال 5: استخدام الترجمات المشتركة
```tsx
<Button>{t('common.save')}</Button>
<Button>{t('common.cancel')}</Button>
<Button variant="danger">{t('common.delete')}</Button>
```

## ⚡ اختصارات مفيدة

### الترجمات المشتركة (common.*)
استخدم هذه المفاتيح في جميع الواجهات:

| المفتاح | الإنجليزية | العربية |
|---------|------------|---------|
| common.code | Code | الرمز |
| common.name | Name | الاسم |
| common.nameAr | Arabic Name | الاسم العربي |
| common.description | Description | الوصف |
| common.status | Status | الحالة |
| common.active | Active | نشط |
| common.inactive | Inactive | غير نشط |
| common.save | Save | حفظ |
| common.cancel | Cancel | إلغاء |
| common.delete | Delete | حذف |
| common.edit | Edit | تعديل |
| common.create | Create | إنشاء |
| common.loading | Loading... | جاري التحميل... |

## ❗ ملاحظات مهمة

1. **صيغة المفاتيح**: دائماً استخدم `sectionName.keyName`
2. **إعادة التشغيل**: بعد تحديث الترجمات، أعد تشغيل الحاوية
3. **تحقق من JSON**: تأكد من صحة صيغة JSON (الفواصل، الأقواس)
4. **استخدم المفاتيح المشتركة**: لا تكرر الترجمات، استخدم `common.*`

## 🐛 حل المشاكل

### المشكلة: الترجمات لا تظهر
**الحل**: 
1. أعد تشغيل الحاوية: `docker restart slms-frontend-next-1`
2. احذف cache المتصفح: Ctrl+Shift+R

### المشكلة: تظهر المفاتيح بدلاً من الترجمة
**الحل**:
1. تأكد من وجود المفتاح في en.json و ar.json
2. تحقق من صحة صيغة JSON
3. أعد تشغيل الحاوية

### المشكلة: خطأ في JSON
**الحل**:
1. استخدم محرر يدعم JSON (VS Code)
2. تأكد من الفواصل بين العناصر
3. تأكد من إغلاق جميع الأقواس

## 📞 الدعم

للحصول على دعم إضافي:
1. راجع `TRANSLATION_IMPLEMENTATION_SUMMARY.md` للتفاصيل الكاملة
2. راجع console المتصفح للأخطاء
3. تحقق من ملفات الترجمة: `locales/en.json` و `locales/ar.json`

---

**آخر تحديث**: {{ التاريخ }}
**الحالة**: ✅ جاهز للاستخدام
