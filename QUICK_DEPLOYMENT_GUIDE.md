# دليل النشر السريع - SLMS
# Quick Deployment Guide

## 🎯 المفهوم الأساسي | Basic Concept

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│   جهازك (localhost)          السيرفر (alhajco.com)         │
│   ─────────────────          ─────────────────────          │
│                                                             │
│   ┌─────────────┐            ┌─────────────────┐            │
│   │ Development │            │   Production    │            │
│   │   الإصدار   │   ──Git──► │   الإنتاج       │            │
│   │   التجريبي  │    Push    │   للعملاء       │            │
│   └─────────────┘            └─────────────────┘            │
│                                                             │
│   localhost:3001             alhajco.com                    │
│   قاعدة بيانات منفصلة        قاعدة بيانات منفصلة            │
│   بيانات اختبار              بيانات حقيقية                  │
│                                                             │
│   ✅ أي تغيير هنا            ✅ لا يتأثر بما تفعله          │
│   لا يظهر للعملاء            على جهازك المحلي               │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## 📋 الخطوات العملية | Step by Step

### الخطوة 1: اختر مزود الاستضافة | Choose VPS Provider

| المزود | السعر الشهري | الموقع | الأفضل لـ |
|--------|-------------|--------|-----------|
| **DigitalOcean** | $24 | أمريكا/أوروبا | المبتدئين - لوحة سهلة |
| **Hetzner** | €15 | ألمانيا | أوروبا - أرخص |
| **Contabo** | €12 | ألمانيا | الميزانية المحدودة |

**المواصفات المطلوبة:**
- نظام: Ubuntu 22.04 LTS
- ذاكرة: 4GB RAM (الحد الأدنى)
- مساحة: 80GB SSD
- المعالج: 2 vCPU

### الخطوة 2: إعداد السيرفر | Server Setup

```bash
# 1. اتصل بالسيرفر عبر SSH
ssh root@YOUR_SERVER_IP

# 2. حدث النظام
apt update && apt upgrade -y

# 3. ثبت Docker
curl -fsSL https://get.docker.com | sh

# 4. ثبت Docker Compose
apt install docker-compose-plugin -y

# 5. أنشئ مجلد المشروع
mkdir -p /opt/slms
cd /opt/slms

# 6. انسخ الكود من Git
git clone https://github.com/YOUR_USERNAME/slms.git .
```

### الخطوة 3: إعداد النطاق | Domain Setup

في لوحة تحكم Namecheap/GoDaddy:

```
Type    Host    Value               TTL
─────   ────    ─────               ───
A       @       YOUR_SERVER_IP      300
A       www     YOUR_SERVER_IP      300
```

### الخطوة 4: إعداد SSL | SSL Setup

```bash
# ثبت Certbot للشهادة المجانية
apt install certbot python3-certbot-nginx -y

# احصل على الشهادة
certbot --nginx -d alhajco.com -d www.alhajco.com
```

### الخطوة 5: النشر الأول | First Deployment

```bash
cd /opt/slms

# اجعل السكربت قابل للتنفيذ
chmod +x scripts/*.sh

# انشر!
./scripts/deploy.sh
```

## 🔄 التحديثات اليومية | Daily Updates

### من جهازك المحلي:

```powershell
# 1. طور وجرب محلياً
docker-compose up --build

# 2. بعد التأكد، ارفع للـ Git
git add .
git commit -m "وصف التحديث"
git push origin main

# 3. انشر على السيرفر (طريقتين)

# الطريقة الأولى - من Windows:
DEPLOY-TO-PRODUCTION.bat

# الطريقة الثانية - SSH مباشرة:
ssh root@alhajco.com "cd /opt/slms && ./scripts/deploy.sh"
```

## 🛡️ الحماية | Safety Features

### النسخ الاحتياطي التلقائي:
- **يومي**: آخر 7 نسخ
- **أسبوعي**: آخر 4 نسخ  
- **شهري**: آخر 12 نسخة
- **قبل كل تحديث**: نسخة احتياطية تلقائية

### في حالة الطوارئ:
```bash
# استعادة من آخر نسخة
./scripts/restore-backup.sh /backups/latest.sql.gz
```

## 📱 المراقبة | Monitoring

```bash
# حالة الخدمات
docker compose -f docker-compose.prod.yml ps

# السجلات
docker compose -f docker-compose.prod.yml logs -f

# صحة النظام
curl https://alhajco.com/api/health
```

## ❓ أسئلة شائعة | FAQ

### هل التغييرات المحلية تظهر للعملاء مباشرة؟
**لا!** يجب عليك:
1. رفع الكود للـ Git
2. تشغيل سكربت النشر

### ماذا لو حدث خطأ بعد التحديث؟
السكربت يأخذ نسخة احتياطية تلقائياً قبل كل تحديث.
استخدم `./scripts/restore-backup.sh` للاستعادة.

### كيف أختبر التغييرات قبل نشرها؟
1. جرب على `localhost:3001` أولاً
2. تأكد كل شيء يعمل
3. ثم انشر

### هل أحتاج إيقاف الموقع للتحديث؟
التوقف عادة 30-60 ثانية فقط أثناء إعادة التشغيل.

---

## 🚀 ابدأ الآن!

1. اشترِ VPS من DigitalOcean ($24/شهر)
2. أنشئ Droplet بـ Ubuntu 22.04 + 4GB RAM
3. اتبع الخطوات أعلاه
4. 🎉 موقعك جاهز!

**الوقت المتوقع:** 2-3 ساعات للإعداد الأول

---

*تم إنشاؤه بواسطة SLMS Deployment System*
