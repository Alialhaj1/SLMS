SLMS Frontend (Next.js)

Minimal Next.js app for the SLMS login UI.

Quick start:

1. cd frontend-next
2. npm install
3. NEXT_PUBLIC_API_URL=http://localhost:4000 npm run dev

The app runs on port 3001 by default.
